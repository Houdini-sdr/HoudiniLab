# HoudiniLab BACKLOG — AP-### (RENEWLab application suites)

The application lane's tracker: the Python / MATLAB / C++ Sounder dev suites
and the WEBGUI dashboard, adapted for Houdini SDR. Conventions live in the
houdini-agents repo (`conventions/tracker.md`) — one canonical id per item
everywhere; durable items only; closed rows collapse to a one-line ledger and
point at continuations; failure rows record behaviors only unless a cause was
traced. Cross-lane needs are AP rows tagged for the receiving lane
(`[→ propose SH ticket]` driver, `[→ propose HS ticket]` gateware,
`[→ propose OS ticket]` image, `[→ propose AG ticket]` workspace) — never
written into another lane's tracker.


> **TEST BASELINE [user, 2026-08-28]: fpga v1.30 or newer, only.** The v1.20 TDD baseline is RETIRED.
> Rows below that record results against fpga v1.20 / device+host v0.2.0 (notably AP-3's max-rate
> 99.998 % and AP-5's V5 tone leg) were validated on a SUPERSEDED stack and are NOT re-confirmed on
> v1.30+. Re-run before citing them as current. Live stack 2026-08-28, both nodes agreeing: fpga 1.30
> `c88e0b5f`, device 0.2.2 `71bcbc6b`, host 0.2.2 `c20d7975`, proto 1.0. `sounder` now prints the
> per-node stack at bring-up and warns `VERSION SKEW:` when participating nodes disagree.

> **Table convention, and the drift in it [2026-09-02].** Rows up to AP-24 fill
> the Status cell. From AP-25 on the status is carried instead as bold lead text
> at the head of the Item cell ("DONE + VERIFIED", "CLOSED ON SILICON",
> "SUPERSEDED BY"), and the Status cell is often absent. Both forms are in use
> below; read the lead text first. A literal `|` in a cell splits the row, so it
> is written `\|` (four rows had lost cells to an unescaped one, AP-43 among
> them, which left it reading as having no state at all).

| Pri | Id | Item | Status |
|---|---|---|---|
| P1 | AP-1 | **rx-recorder: timed RX capture to HDF5 via the Houdini SDR.** New lean CC/Sounder tool (`rx-recorder` binary) that opens the SoapyHoudiniSDR device (`driver=houdinisdr`), applies rate/freq/gain from a JSON config, activates a continuous RX stream NOW, records X seconds of CS16 samples to an HDF5 file, and reports timing gaps/overruns. No beacon/TDD/Iris machinery — single-channel continuous RX is the validated device path. Includes a small refactor: `RecorderThread` decoupled from the sounder `RecorderWorker`/`Config` via a worker interface, so the recorder pipeline is reusable. Branch `feat/rx-recorder`. **SILICON-VALIDATED 2026-07-10 (rig B, .64 GB10 aarch64 -> node .22, fpga v1.19 / device 2695498b / host a040debf):** 30.72 MSPS 938/938 slots AND 245.76 MSPS (top rung, 983 MB/s) 7500/7500 slots -- both 2 s, zero drops/gaps/overflows, `TOTAL_UNTRUSTED_SAMPLES=0`, payload = live ADC noise floor; radio-free ctest suite also green on aarch64. Measured bring-up latency START_HW->FIRST_SAMPLE ~98 ms (validates recording both). Merge to develop DEFERRED [user 2026-07-10]: work continues on **`arc/rx-recorder`** (renamed from `feat/rx-recorder` per the staged-arc shape — future features fold in as `feat/*`; develop rippled in to stay synced); merge discussion later. | VALIDATED — branch active |
| P2 | AP-2 | **Sample-drop recording fidelity: app side landed; driver break-at-gap proposed.** [user] Investigation confirmed the driver's `Read()` (houdini_stream.cc) splices ring slots across packet-tick discontinuities with no continuity check — kernel-UDP loss silently time-shifts every sample after a mid-read gap under the buffer's single leading `timeNs`; per-packet `tickCount` truth exists in slot metadata but is never compared. Evidence + options: `docs/RX_GAP_AWARENESS.md`. **App side (landed on `feat/rx-recorder`):** sample-time grid anchored at `FIRST_SAMPLE_TIME_NS`, forward gaps repaired with placeholder zeros (grid never drifts), all untrusted regions recorded in `/Data/Gaps` `{start_sample,n_samples,start_time_ns,cause}` + `TOTAL_UNTRUSTED_SAMPLES`; extents read-widened today, auto-tighten to sample-exact via proposed `rx_gap_break=1` capability kwarg. **`[delivered → SH-253]`** (data-integrity class): break-at-gap readStream contract per the doc — detection is one predicted-tick compare on already-parsed headers, preserving the ring's kernel-offload role. **OBSERVED LANDED 2026-07-10:** host build `778497c2` (02:35Z) advertises `rx_gap_break=1` and rx-recorder auto-engaged exact-extent mode via the capability probe, zero app changes — as designed. Lossy-rate validation of exact extents DONE 2026-07-12: the max-rate direct-path baselines carried 22–25 % real loss with every extent sample-exact and cross-verified program↔inspect (and the V4 forensics leaned on that exactness to root-cause the loss — the accounting is load-bearing, not just honest). | app DONE — exact extents validated under real loss |
| P2 | AP-3 | **Max-rate RX capture: support + test 1.96608 GSPS (~62.9 Gbps).** [user] Goal: rx-recorder sustains the RX max streaming rate — `RX_FAB_CLK` 245.76 MHz × 8 samples/cycle = 1966.08 MSPS CS16 ≈ 62.9 Gbps (vs the 245.76 MSPS top rung of the standard `setSampleRate` ladder). Knowns: sustained ~31.5 Gbps already sees 10–15 % kernel-socket drops on a single UDP socket (SoapyHoudiniSDR README), so 62.9 Gbps needs host-path work (socket strategy is driver-side; app side: RAM-resident ring sizing — disk cannot sustain ~7.9 GB/s, capture-then-drain), and AP-2's gap accounting is the honesty mechanism at any loss rate. Test on rig B (GB10). Multi-channel concurrent capture (2 independent single-channel streams, ports 10001/10002) is a related later extension — pipeline already antenna-indexed. Budgets + copy-chain analysis + order of operations: `docs/RX_MAX_RATE.md`. **Measured 2026-07-10:** rig-B disk (Phison E27T DRAM-less Gen4, 4 TB TLC) sustains **4.3 GB/s over 40 GiB O_DIRECT** (pSLC not exhausted -- better than the 1-1.6 GB/s class estimate, still < the 7.86 GB/s max rate ⇒ RAM-window plan stands); 245.76 MSPS (983 MB/s) capture ran clean end-to-end (AP-1 validation). **Capture support DONE + tested 2026-07-10** (folded to the arc, merge 543376f): the RX_FAB_CLK walk reaches 1.96608 GSPS (`listSampleRates` rescales with the clock — confirmed live), 8 s RAM-window capture end-to-end with 0 host drops / 0 overflows. **Measured wall: steady-state ~35 % kernel-socket loss (~40 Gbps sustained goodput on one UDP socket, NOT startup-heavy)** — everything the kernel delivered was captured, loss exactly accounted in `/Data/Gaps`. Grid tolerance rate-corrected (integer-ns quantization above ~1 GSPS). Observation for fpga/sw: RFDC ADC0.1 FIFO_OVR interrupt storm (~500k/session) at the high-Fs config, capture data unaffected. Remaining (lossless sustained 62.9 Gbps) is driver-side transport work — see `docs/RX_MAX_RATE.md` §single-socket-drop-diagnosis (measured: ALL drops are socket-buffer-full/RcvbufErrors; .64 `rmem_max` at half prescribed): **`[superseded → SH-258; busy-poll facet → SH-265 (3); residual UDP_GRO + port-striping undelivered, reduced-pri]`** the pre-AF_XDP rung — `SO_BUSY_POLL`/`SO_PREFER_BUSY_POLL` + `UDP_GRO` in `stream/udp_comm.cc`, and port-striped multi-socket (device side needs HS). App-side harness `tools/rx_drop_probe.sh` + [user] root one-liners staged for the next quiet rig window (T2 affinity + T4 tuning + 180 ms lossless-burst demo). **Addendum (fold 8ad32c6):** rate walk made symmetric (halves the clock toward slower rates [user]); live-confirmed the exact clock menu (15.36–245.76 MHz, fail-loud floor), `listSampleRates` post-move advertises only fab×vld (sub-30.72 MSPS genuinely unavailable), **491.52 MSPS validated fully clean** (7500/7500, zero gaps), ADC0.1 FIFO-storm hypothesis recorded (tile-level power-up leaves the sibling block unmanaged through the clock change — untraced, [user]). **OBSERVED 2026-07-12: the software lane shipped the AF_XDP tier itself** (SH-258 on their `feat/sh258-afxdp-ingest`, silicon-legged 99.92 % captured at 1966.08 MSPS with the SH-259 paired geometry `HOUDINI_MTU=3512` + NIC MTU 3498 + `rx_xsk=require`); the pre-AF_XDP handoff (busy-poll/UDP_GRO/port-striping) is now socket-fallback-scope only, reduced priority. App side staged (fold b65c812): **releasable demo config `files/rx-record-demo.json`** (1 ch @ 1.96608 GSPS, require-mode zc chain; host provisioning = [user] caps bundle + NIC MTU one-liners, `rx_xsk_nic_reconcile` deliberately unused on the shared rig) + validation matrix V1–V5 in `docs/RX_MAX_RATE.md` for the next quiet window ([.64 frees when the software lane's regression testing/release completes). **RIG-VALIDATED 2026-07-12 (TDD baseline fpga v1.20 `da4c9d4c` / device+host v0.2.0, `tdd=1` — a superset carrying SH-254/258/259):** the demo zc chain ENGAGES (`rx_xsk_nic_reconcile=1` set NIC 9000→3498 set-and-leave, `zero-copy RX engaged q19`); first runs lost ~22–25 % — **root-caused the same day (V4 CLOSED)**: per-packet acquire/release consumer overhead at the 848-sample zc quantum (2.32 M acquires/s; sustains ~78 % of wire) + the 256 MB UMEM's 28 ms absorb window (loss was a ring-occupancy sawtooth, not a steady deficit) + 50 ms starvation-exit stalls from the worker's poll timeout. NOT core placement, NOT memory bandwidth (both ruled out by config-only A/B); the earlier "~15.7 GB/s consumer throughput" attribution is superseded. Fix shipped in the demo config: `direct_rx=off` (readStream consumer; still sample-exact via rx_gap_break) + `ring_bytes=1 GiB` + worker pinned off the NIC-queue IRQ core. **V3 VALIDATED 2026-07-12: 8 s @ 1.96608 GSPS = 99.9979 % captured** (15000/15000 slots, one 0.166 ms start-transient gap, NIC-ledger exact: `rx_out_of_buffer` +385 pkts × 848), all loss in `/Data/Gaps`. Full causal record + A/B ladder: `docs/RX_MAX_RATE.md` §V4; operator runbook `CC/Sounder/RX_DEMO_WALKTHROUGH.md`. Driver refinement asks spun out to **AP-7**; the socket-fallback SH ask stays reduced-priority. | max-rate VALIDATED 99.998 % — retire at merge-develop |
| P3 | AP-4 | **Zero-copy RX read path: driver direct-buffer API.** [user] Collapse the host copy chain for max-rate capture: the plugin's SPSC slot ring (`stream/ring_buffer.h`) already has the exact shape for SoapySDR's sanctioned `acquireReadBuffer`/`releaseReadBuffer` direct-access API — implementing it is close to refactoring `Read()`'s slot-arm loop into public surface. App side: rx-recorder gains an acquire/release assembly path (one copy: ring slot → HDF5 row, where AP-2's zero-insertion already happens), readStream fallback kept. Bonus: per-acquire = per-packet tick ⇒ sample-exact gap accounting independent of AP-2's break-at-gap contract; the two SH asks touch the same code and could land as one package. Ruled out: app-donated recv buffers (no standard API), moodycamel in the driver ring (SPSC ring is the cheaper primitive), AF_XDP (deferred until measurement demands — see `docs/RX_MAX_RATE.md`). **`[delivered → SH-254]`** **OBSERVED LANDED 2026-07-12:** SH-254 shipped on `feat/sh258-afxdp-ingest` exactly as proposed — one acquire = one packet + per-packet tick stamp, probe = `getNumDirectAccessBuffers > 0`, out-of-order release OK, works on both ingest paths. **App consumer DONE (fold b65c812):** `SampleSource` state machine (`include/rx_recorder_capture.h`, radio-free-tested 12 scenarios), `DirectBufferSource` (≤1 held, release-on-drain, dropped before deactivate), `direct_rx` knob auto/require/off, sample-exact extents structural in direct mode (readStream keeps the rx_gap_break probe, now also honoring the per-stream knob), READ_PATH/GAPS_EXACT/STREAM_ARGS file provenance. Remaining = silicon validation V1/V2. **V1 DONE 2026-07-12 (TDD baseline):** 245.76 MSPS 2 s clean (7500/7500, `TOTAL_UNTRUSTED_SAMPLES=0`, `READ_PATH=direct`, `GAPS_EXACT=1`) — the direct-buffer path engaged even on the kernel-socket ingest, so SH-254 + SH-258 compose. **V4 finding 2026-07-12:** at the max-rate zc geometry (848-sample packets ⇒ 2.32 M acquires/s, 431 ns/packet budget) the per-packet acquire/release path sustains only ~78 % of wire — per-rate guidance settled: `direct_rx` auto at ladder rates (free, structurally exact), `off` in the max-rate demo (readStream amortizes; exactness held by rx_gap_break). Batch acquire/release ask → **AP-7**. V2 two-path cross-compare deprioritized (both paths independently validated exact, clean and lossy legs). | app DONE — V1 validated; per-rate path guidance settled |
| P2 | AP-5 | **TX replay loopback tone for capture verification [test-only].** [user] Arm the deployed stack's TX replay path so rx-recorder records a KNOWN signal over the DAC→ADC loopback instead of bare noise floor — verification only, not a capture feature. Driver surface (already validated, SH-124/126 bin-exact): load the tone via `writeStream` on a `tx_mode=replay` stream (`TX_REPLAY_ENABLE` retired SH-143; continuous RTL loop; ch0=DAC0.0, ch1=DAC2.0; `houdini_setup.py::tx_iq_tone` snaps freq to a replay-RAM bin; mixer conjugates the frequency axis). Verification value: bin-exact tone frequency + amplitude sanity + **slot-boundary phase continuity as an independent cross-check of AP-2's gap accounting** (any uncounted drop = phase jump). Shape SETTLED [user 2026-07-10]: the capture process's own device handle must own BOTH streams — an rx-recorder config section (`"tx_replay"`: freq/amplitude/channel, off by default). Out-of-band arming is not viable: the tone's lifetime = the replay stream's lifetime (closeStream/destructor releases the DAC tile refcount, leaked refs swept), so the armer must outlive the capture, and concurrent second-handle operation is out-of-contract (SH-251 multi-client note); the validated loopback flow (`rx_tx_loopback.ipynb`) is single-handle. Prereq: confirm rig-B DAC→ADC loopback cabling state. **Analysis component LANDED (fold fdb3ff5):** `CC/Sounder/tools/inspect_rx_record.py` — the plot_hdf5-style inspect/deep-inspect for the rx-recorder schema (trust report from `/Data/Gaps`, payload sanity, RMS timeline, spectrum) whose `--tone` mode is the AP-5 HIL verification core: slot-boundary phase continuity of the loopback tone cross-checked against the gap table (unexplained jump = missed samples, exit 1; tone-prominence guard for tone-less captures). Verified on rig B against gap-rich synthetic + live captures. **TX arm DONE (fold b65c812):** the `"tx_replay"` config section (freq/amp/channel/n_addrs, off by default) — the capture's own handle arms `tx_mode=replay`, tone bin-snapped to the n_addrs loop (seamless wrap = the property `--tone` leans on), loaded via writeStream (1 element = 1 u32 replay-RAM word, depth 4096), RAII deactivate+close after capture; `TX_REPLAY_FREQ_ACTUAL/_AMP/_CHANNEL` recorded; starter config `files/rx-record-tone.json` (245.76 MSPS clean rung). Remaining: confirm rig-B DAC→ADC loopback cabling, then the V5 HIL run: tone on → capture → `--tone` verdict clean. **V5 DONE 2026-07-12 (rig-B DAC0.0→ADC0 loopback confirmed cabled [user]):** tone armed 19.92 MHz (bin-snapped from 20 MHz) on the capture's own handle, 245.76 MSPS clean rung; `inspect --tone auto` detected −19.92 MHz @ 87.9 dB, 7499 boundaries, **0 unexplained phase jumps**, exit 0 — first on-silicon exercise of the AP-5 arm path, and an independent confirmation that the gap accounting is complete. Demo-rate tone leg queued for the next quiet rig window ([user] runs) — now high-value: on a 99.998 % capture the tone should show exactly ONE explained phase jump at the single padded extent, the strongest possible gap-accounting cross-check. | DONE — V5 clean-rung leg verified; demo-rate leg queued |
| P3 | AP-6 | **Correlator: config-driven `HOUDINI_MTU` pass-through so it shares the small-frame NIC geometry.** [user] The Sounder UE beacon-correlator (`find_beacon_avx`/`find_beacon_cuda`, the full `sounder` binary; [user]'s branch `feat/correlator-rate-test`) opens the device at `SoapySDR::Device::make(args)` (`Radio.cc:117`, `BaseRadioSet.cc:46`) with the default 8192 wire geometry, so its RX frames (~8186 B) need the data NIC at MTU ≥8186 (the 9000 setting). rx-recorder's zero-copy demo needs the paired-small geometry (`HOUDINI_MTU=3512` → NIC 3498) because AF_XDP single-buffer ZC refuses a NIC MTU that exceeds one 4 KiB chunk (confirmed on silicon 2026-07-12: XDP attach `Invalid argument` at NIC 9000). The two cannot share one NIC MTU today ⇒ a set-and-leave MTU dance on the shared rig. **Fix:** thread a `HOUDINI_MTU` value from the correlator's `Config` into the make-args (mirrors rx-recorder's `device` kwargs), default unset (8192); at 122.88 MSPS (~492 MB/s) the kernel-socket path handles the small-frame geometry trivially (no AF_XDP needed), so the correlator can run at NIC 3498 and the dance disappears. Coordinate with [user] — the change lands on the correlator branch, not the arc. | proposed [user] |
| Hi | AP-8 | **UE fine-grained timed TX for the sounder closed loop.** BS native-TDD receive done (records the gated pilot non-zero); the UE needs to place its pilot in the BS `rx_gate` (0.5 ms symbol, 33 µs recorded, ≤0.5 ms tolerance) at a beacon-referenced time without the whole-ms 1 ms drift-cliff. **RESOLVED by SH-301 (relay): the capability already exists — streaming TX accepts the 3.125 µs TDD grid via the `tdd=1` stream arg (SH-248, in the deployed driver); no driver change needed.** My hand-off premise was partly stale (mis-cited the device replay path `:1187` / `setHardwareTime` `:1846`; the streaming timing is the host `TxTickAnchor`, missed). **Remaining = app-side:** set `tdd=1` on the UE `tx_stream_args` in `ClientRadioSet` (streaming mode; drop the method-1 replay-strobe changes), then run the full sounder closed loop (UE pilot on the 3.125 µs grid → BS gate). See `docs/UE_TX_FINE_GRID_TIMING.md` (RESOLUTION at top). ⇄ SH-301, SH-248. Renumbered from AP-1 at the develop merge (develop's canonical AP-1 = rx-recorder). | app-side, open |
| P3 | AP-13 | **Role-layer gaps found by the 2026-08-28 bench session.** `[→ propose AG ticket]` **(1) The ship-and-run procedure should be a skill.** Bundle -> scp -> `git fetch`/`merge --ff-only` on the rig -> rebuild -> launch -> verify ran SIX times this session out of `memory/application/rig-b-app-ops.md`. It is a repeated, proven procedure with exact commands, which is the bar for a skill; keeping it in memory means every session re-derives it and can get it wrong. It should fail closed on the two traps hit today: launch/kill by process GROUP (an orphaned wrapper started a second sounder against the same radios and the dashboard silently interleaved both), and never `pkill -f <pattern>` over ssh (the pattern matches the remote `bash -c` running it and kills the session mid-command). Verify step = `pgrep -cx sounder` is exactly 1. **(2) Role brief is silent on device-server recovery.** `roles/application.md` says hardware experiments run against the already-deployed stack and to request stack changes from the owning lane. Restarting `SoapySDRServer` on a radio is the documented operator recovery for a stuck stream or RPC timeout (it is in [user]'s own run notes and now in `CC/Sounder/CSI_DEMO_WALKTHROUGH.md` 8.4), and this lane needed it twice today. The brief should say plainly that this is an application-lane action, not reaching across, so the next session does not hesitate. **No bloat found:** branching, tracker, handoffs, commit-style and session-open were all load-bearing this session; commit-style's no-AI-attribution and no-dates rules in particular were applied directly. | proposed |
| P3 | AP-25 | Timestamp-derived BS slot extraction: replace the energy-centroid slot tagging in houdiniTddRx with arithmetic from the RX stamp + epoch (row 3.5 proved the mapping is exact). Removes the centroid draw class entirely and simplifies the receiver; was the root-cure candidate for the AP-15 pilot-slot ISI layer (interim fix superseded by MTS closure, mechanism still worth removing). |
| P3 | AP-26 | View/record tandem + replay: today HOUDINI_CSI_UDP makes view and HDF5 recording mutually exclusive (ledger 4.46), so viewed data is never inspectable by the deep-inspect tooling. Wanted [user 2026-08-30, "keep in mind"]: record+view in tandem, and/or a play mode replaying a recorded HDF5 through the view as live data. DEFERRED until data validation and timing work complete. |
| P3 | AP-28 | Tick-native TX scheduling [-> propose SH ticket when user says go]: a time_ticks=1 stream-arg opt-in making the host plugin interpret writeStream time as raw ticks (the wire already carries ticks; live-TX never crosses SoapyRemote; precedent = tdd=1/SH-248). Kills the 3125 ns ns-exactness quantization at the API seam; floor = the 8-tick TX start grid, so pair with a <=7-sample leader for sample-exact, or ask fpga lane whether any even tick is honorable. Evidence: DEMO_VERIFICATION.md 4.48/4.49. The zero-padded burst (shipped) already achieves sample-exact placement, so this is simplification/generalization, not a blocker. |
| P2 | AP-29 | Streaming-native GUI history panels [user 2026-08-30]: the dashboard has one history view (waterfall); add scrolling strip charts (chart-recorder style) per card for mean \|H\| dB, anchored phase at a reference tone, H-stability median, and CNS score, so trends and events read at a glance. Keep the zero-dependency canvas idiom (self-contained page is load-bearing); if interactivity is later wanted, vendor uPlot (streaming-native, tiny) like tabler.min.css -- not Plotly/Chart.js. Raynet is batch-shaped (groups -> finished figures); ours is streaming and needs state panels + history panels. |
| P1 | AP-52 | **TWO DEFAULTS CHANGED 2026-09-02 [user: 'a conservative value, maybe 10x'], AND THE GATE MUST RE-RUN.** `HOUDINI_SYNC_RESIDUAL_PPM` 1.0 -> 0.1 (cadence 260 ms -> 2.6 s) and `HOUDINI_SCATTER_TOL_US` 8.3333 -> 2.0 (gate 1024 -> 246 samples). Both are conservative against the measurement, deliberately: the ADEV says the real post-tracking residual is 0.002-0.008 ppm so 0.1 keeps 12-50x margin (the full margin, 0.01 ppm and a 26 s cadence, is NOT taken because 20 s is where the data ends); and the measured detection residual is -2 to +3 samples so 246 keeps 41x margin, with 2.0 us being the only tighter value that has actually been run on silicon. **THE GATE AT DEMO_VERIFICATION 8.79 COVERED THE OLD PATH.** Neither default has 3 clean runs in its new position, and the gate CRITERIA need restating first: a 10x cadence gives ~10x fewer detections per run, so accept counts fall from ~100 to ~10 and the residual sd is computed over far fewer samples. **GATE RE-RUN DONE AND PASSED 2026-09-02.** Criteria restated FIRST and committed before the run (DEMO_VERIFICATION 8v), then six 60 s runs interleaved on one node stack and one binary: 0 escalations and 0 off-grid in all six, acquisition confirmed in all six at residual -1 to +1, **18/19/19 accepted detections POST against 19/19/19 PRE, where the geometry predicted 'about 18' before the run**, max abs(residual) 2-14, residual sd 3.79 POST against 3.60 PRE (ratio 1.05). One pre-registered criterion was corrected before the run and the correction is recorded: G4's 6-sample bound came from measurements at the OLD cadence, and the residual between looks IS the drift between them, so a 10x cadence makes it 10x larger by arithmetic -- 25 samples at the measured eps. **A FIRST ATTEMPT WAS BINNED**: the software lane rolled a device+host build onto the boards between runs 2 and 3, every run stayed green, and nothing said the baseline had moved. `gate_summary.py` now exits 3 when runs disagree on fpga_commit/device_build/host_build/beacon. Two other AP-52 items remain unshipped -- the escalation thresholds (knobs landed, swept clean, defaults unchanged) and the alpha-beta gains (unswept). DEMO_VERIFICATION 8u. ORIGINAL: **KEEP THE SOFTWARE TIME TRACKER; ITS ROLE CHANGES, IT IS NOT SUPERSEDED [user asked whether to retain it].** The clock steer is the OUTER loop and the tracker is the INNER one, which is the standard cascaded arrangement, and five things make the inner loop non-optional. (1) **It IS the sensor** -- the steering loop's error signal is eps, and the tracker is the only thing that measures it, so removing it removes the actuator's input. (2) **Steering fixes RATE, never OFFSET** -- propagation delay, cable and RF-chain latency are a static time offset that no frequency correction touches; the UE still has to know where the BS frame starts, which is what `houdini_pilot_ref` is. LTE keeps timing advance on perfectly frequency-locked networks for exactly this reason. (3) **Bandwidth** -- the steer is deliberately slow (30-60 s, and slower still for AP-51), so re-acquisition, beacon glitches and post-escalation resync are all inner-loop work; the clock cannot respond within a frame. (4) **Quantum and range** -- 0.1251 ppm per count leaves +-0.065 ppm the software must absorb, and outside +-60 ppm, or on any node not in `calibrated` mode where CLOCK_ADJ is refused outright, software is the only path. (5) **Doppler** -- AP-51 requires the steer NOT to chase motion, so under Doppler the inner loop carries the fast-varying part while the outer loop holds. That division of labour IS the invariance. **What CAN go now that backward compatibility is not required** [user]: the escalation thresholds were tuned for a grid that walked out in ~1 s and are absurdly conservative when it now holds for minutes -- retune, do not remove the net; the +-kScatterTol gate should finally be demoted from control law to a much TIGHTER outlier reject, since the steered residual is tiny; the alpha-beta gains and the resync cadence were both tuned against 8.5 ppm and should be re-derived for the steered regime; and AP-42 (UE TX frequency pre-compensation) is genuinely superseded, because steering the reference moves the TX NCO with it -- keep it only for non-steerable nodes. -> AP-47, AP-51, AP-41, AP-42. \| KNOBS LANDED, defaults unchanged; the sweep is leg 9 | 2 of 4 items SHIPPED, gate re-run required; thresholds and gains still open |
| P1 | AP-53 | **(c) FIXED, (d) CLOSED 2026-09-02, (a) and (b) STILL OPEN.** (d) **DID NOT REPRODUCE and is closed:** 8.51 reported sync residual sd rising to 2.17 with max 14 and could not explain it; the PRE arm of this session's control IS essentially that code and reads sd 0.58-1.08, max 2-4. Transient, and NOT caused by the phase fix 8.51 suspected (8.80). (c) done, SYN1 carries both CFO figures. **(a) CLOSED 2026-09-02, REPLICATED 3x, and it changes a shipped default.** With the binning fixed, three 300 s captures give a proper ADEV bathtub: minimum at **tau = 2 s** in all three (0.00185 / 0.00234 / 0.00201 ppm), drift there 0.46 / 0.58 / 0.49 samples, and at tau = 20 s the drift is 18.1 / 23.5 / 20.3 samples against our 32-sample budget. 8.30's 'flat ~48 samples, measurement floor' was entirely the tool. **The clock supports coasting ~20 s where we resync every 0.26 s, so the cadence default is ~75-100x too aggressive. THE CHANGE HAS NOT BEEN MADE** -- this is a measured finding awaiting a decision, not a shipped fix. (b) Campaign legs A/B/C still came from an earlier instrument build than leg D; re-run the ladder on one build before quoting it outside the lane. Both need a rig window. | (c)(d) DONE; (a)(b) OPEN, rig-blocked |
| P1 | AP-45 | **ARCHITECTURE [user]: the UE conforms to the BS; the BS must not run a control loop per UE.** With multiple UEs a BS cannot chase each one's timing or frequency, and it does not have to: LTE/5G broadcast a reference, each UE derives time AND frequency from it and PRE-COMPENSATES its own uplink (timing advance + frequency pre-compensation) so every UE arrives already aligned. Consequences for us: (a) **the timing side is already architecturally right** -- our tracker is UE-side and the UE conforms, so it scales to N UEs unchanged; (b) **the durable frequency answer is UE-side pre-compensation (AP-42), not BS-side correction** -- this promotes AP-42 above AP-38 as the primary mechanism; (c) AP-38 stays, but reclassified as RECEIVER EQUALIZATION rather than a control loop -- the BS estimates H per UE anyway, so a per-UE residual pilot-phase term is part of that estimate and scales fine, exactly as every 802.11/LTE receiver does it. (d) The UE's only DL reference today is the BEACON, which is sufficient for timing (proven) and for frequency (the shared LMK reference makes the tracked timing rate the carrier offset). DL data symbols do not exist yet; when they do the same two-state estimator gets a richer reference and nothing about the scheme changes. That is what makes it durable. -> AP-38, AP-42, AP-31. |
| P2 | AP-46 | Prior art for AP-38 is already IN THIS REPO, at both tiers, and should be followed rather than reinvented: `PYTHON/IrisUtils/ofdmtxrx.py` has `sfo_correction()` (least-squares phase SLOPE across the pilot subcarriers, i.e. the timing/SFO term) and `phase_correction()` (the common-phase term), applied in sequence by `PYTHON/DEMOS/SISO_OFDM.py:375-381`. `MATLAB/rl_ofdm_siso.m` does the same with `DO_APPLY_PHASE_ERR_CORRECTION`. **Our C++ already computes the slope and discards the intercept**: `recorder_worker.cc` stage 2 least-squares-fits pilot phase vs subcarrier index and keeps only `slope`, throwing away exactly the common-phase term the reference implementations keep. The HDF5 inspection path carries the metadata for it (`OFDM_PILOT_SC`, `OFDM_PILOT_SC_VALS` in `hdf5_lib.py`). -> AP-38. |
| P1 | AP-51 | **DOPPLER INVARIANCE NEEDS A TWO-WAY MEASUREMENT, AND WE ALREADY HAVE BOTH HALVES [user: must not break OTA].** Correcting an earlier claim of mine: I said the timing channel is Doppler-immune because Doppler shifts the carrier and not the sample clock. **That is wrong.** A range rate v/c changes the propagation delay continuously, which appears as an apparent time scaling indistinguishable from a clock rate offset -- a Doppler shift IS a time dilation. On a ONE-WAY link the two are physically degenerate: timing sees (eps + v/c), carrier sees (eps + v/c)*f_c, the same fraction both times. No single-link estimator, at any lag, can separate them. **The separation is two-way, the standard two-way time/frequency transfer result:** the UE measures the downlink beacon arrival rate (+eps + v/c) and the BS measures the uplink pilot arrival rate (-eps + v/c), because the clock term flips sign between directions and the range-rate term does not. **DIFFERENCE = 2*eps (clock), SUM = 2*v/c (Doppler).** We already produce both observables -- the UE's arrival ramp, and the BS's `pilot_grid_off`, today only a health check. Promoting it to the second measurement in the loop is the Doppler-invariant architecture, and it needs no new signal -- and no protocol either, since the BS RX thread and the UE client thread run in the SAME sounder process, so the reporting path on this bench is a shared value rather than an over-the-air message (a real deployment needs the message; the bench can prove the math first). **REFINEMENT, and the subtlety that would bite an implementer: our uplink half is NOT independent of the downlink half as filed.** Two-way transfer assumes the UE transmits on its RAW local clock, so that the BS-side measurement carries (-eps - rdot/c) independently. Ours does not: the UE transmits on its TRACKED grid, i.e. already corrected by the downlink measurement, so `pilot_grid_off`'s slope is the tracker's RESIDUAL and not the raw uplink rate. With a converged tracker that residual goes to zero and the sum/difference degenerates. The fix is bookkeeping, not a new measurement: **the UE knows exactly what grid rate it applied (`houdini_frame_period`), so adding it back to the BS-measured residual reconstructs the independent uplink rate.** De-embed the applied correction before forming the sum and difference. **Design rule until then: the FAST software tracker may absorb the combined term (it only has to stay locked, not attribute), but the SLOW clock steer must NOT integrate Doppler into the oscillator** -- that would mis-tune the VCXO chasing motion, burn actuator range, and diverge across UEs. Keep the steering time constant long (minutes) and its authority bounded, which separates quasi-static clock error from transient motion in the frequency domain; that is the interim invariance. -> AP-47, AP-41, AP-50. |
| P1 | AP-50 | **STAGE 1 DONE + VERIFIED 3 RUNS: bias -724.7 Hz mean -> within +-69 Hz, mean now straddling zero.** Scatter unchanged (it is the lag-128 thermal floor). Stage 2 DEMOTED, see below. FIX THE BEACON ESTIMATOR [user], three staged parts. What it is FOR after the tracker exists is the thing to be clear about first: it is NOT a second opinion on the clock -- the timing channel already measures eps to 0.036 ppm where the estimator manages 405 Hz sd (0.8 ppm). Its unique value is that it sees things the timing channel structurally CANNOT: OTA Doppler shifts the carrier without touching the sample clock, so timing gives eps, carrier gives eps*f_c + Doppler, and the DIFFERENCE is the Doppler. That is why it gets fixed rather than retired.** (1) STOP FEEDING IT A BAD INDEX -- the whole measured bias is one-sided detector misalignment (AP-39). Better than the +8 guard: **use the grid tracker's PREDICTED beacon position instead of find_beacon's earliest-crossing index.** The tracker predicts to +-3 samples (measured resid sd 0.63-0.70, max 3) while the detector is biased early and scatters; the prediction already exists and costs nothing, and it removes the guess rather than compensating for it. Keep the guard as the fallback for the pre-lock case. (2) EXTEND THE LAG (AP-34b) -- **DEMOTED after working the numbers; it is NOT on the critical path and I filed it too high.** Three reasons. (i) It needs detection PAIRS no more than ~9 frames apart to stay inside what the tracker can unwrap (range = 1/(2nT), so n=9 gives 55.6 Hz against the tracker's ~18 Hz, while our actual detection spacing of ~260 frames gives 1.9 Hz and cannot be unwrapped by anything we have) -- so it is a resync-SCHEDULING change, not just an estimator change. (ii) The ladder cannot bootstrap itself: the lag-128 estimate at sd 400-800 Hz cannot unwrap even a one-frame lag, so only the tracker can, which means the tracker is a prerequisite rather than a beneficiary. (iii) It would buy ~0.05 Hz where the timing channel already gives 18 Hz for the clock and the actuator quantum is 64 Hz -- three orders more precision than anything consumes. **And Doppler separation, the reason to keep a carrier channel at all, is TWO-WAY (AP-51) and uses timing on both links, not a more precise one-way carrier.** Do AP-51 first. Original reasoning: precision scales with lag -- precision scales with it: STS lag 16 gives ~1.7 kHz, gold lag 128 gives ~100 Hz (405 measured at 30 dB), **frame-to-frame at lag 122880 gives ~1 Hz** but only +-500 Hz of unambiguous range. That range is exactly why it was unusable alone -- and is not a problem any more: the tracked clock pins the answer to +-18 Hz, and under AP-47 steering the residual is bounded to +-32 Hz by the actuator quantum. **The tracker is the coarse stage that unwraps the fine one.** (3) Then the two channels are independent enough to fuse (AP-41's Kalman with a Doppler state), which is not safe today because a biased sensor breaks inverse-variance weighting. -> AP-39, AP-34, AP-41, AP-47. |
| P2 | AP-43 | **ANSWERED PROPERLY 2026-09-02 on the software lane's joinless re-arm build (host d3785447).** 30 of 30 arms delivered at leads of 8, 24 and 100 frames with no deactivate, where the previous build refused every second arm at 8 and 24. **arm = 1.6-2.9 ms, and cycle = lead + arm**, so the lead is the caller's choice and the arm is the only unavoidable cost. **The seam is clean:** comparing each burst's armed tick against its returned first-sample tick, 300 of 300 started EXACTLY where armed, zero early and zero late, so the straggler that design accepts did not occur. **The settle stays 0** -- 5 ms is exactly additive on the arm (1.8 -> 6.8 ms), which at an 8 ms lead leaves no margin and cost 5 bursts in 300; it spends the lead rather than buying safety. **VERDICT: per-frame timed DL reads remain out** (a 1.6 ms arm exceeds the 1 ms frame) **and the BEACON case is clearly affordable** -- one arm per 260 ms resync period is 0.6 % overhead for the arm, or 3.5 % for a full 9 ms cycle at an 8-frame lead, and it buys the 97 % fronthaul saving and puts the read where the beacon is. **Build the beacon case.** Three earlier figures from this row are RETIRED: they timed a fresh arm after a host rollback, not a re-arm, and one of them excluded a 50 ms deactivate drain entirely. DEMO_VERIFICATION 8t. ORIGINAL: **ANSWERED ON SILICON 2026-09-02, and the answer is not the binary this row expected. PROMOTED to P2.** The timed-burst ARM costs **1.9 ms mean, 1.87 median, ~1.4 ms floor**, over 300 arms and CONSTANT across `--lead-frames` 4/12/24 -- the read-after-arm tracked the lead exactly (2.2 / 10.4 / 23.0 ms), so it is waiting for the scheduled instant, not transfer, and any 'arm + read' total just reports the lead the caller chose. **Against a 1 ms frame that rules out per-frame timed DL reads.** But this row's PRIMARY case is the BEACON, armed once per 260 ms resync period, where 1.9 ms is **0.7% overhead** -- affordable, and it buys the 97% fronthaul saving AND puts the read where the beacon is, which is the whole point. So: **build the beacon case, drop the per-frame DL case.** Two contract facts learned: an off-boundary start time is refused outright (must be a whole millisecond, or on the 3125 ns TDD grid) with a precise message, and an END_BURST read that SUCCEEDS still needs a fresh activateStream before the next arm -- without a deactivate between bursts exactly half the arms produce nothing (DEMO_VERIFICATION 8.83, 8.86). Probe `tests/demo-verify/burst_rx_cost_probe.py`. **RE-MEASURED on the software lane's END_BURST re-arm fix (device 25eaaec9): arm 1754 us mean over 80 arms with NO deactivate and zero empty reads**, against 1900-2200 us with the workaround, so their fix both removes the workaround and takes ~10% off. **PROVISIONAL:** a follow-up fix adds a 5 ms drain, and if that sits in the ARM path per arm the figure becomes ~7 ms -- which does NOT flip either verdict (the beacon case goes 0.7% -> 2.7% overhead at the 260 ms cadence and stays affordable; per-frame DL goes from clearly out to very clearly out) but would make the number in this row wrong by 4x. Re-measure on their build id rather than scaling it. \\| ANSWERED; arm figure PROVISIONAL pending the software lane's drain change \| ARM FIGURE INVALID (wrong arm); re-measure on the joinless build | ANSWERED; build the beacon case, per-frame DL dead |
| P3 | AP-44 | Two unexplained observations, both recorded rather than theorized. (a) **Loop-rate run-to-run spread**: 412 vs 746 iter/s on the same binary, and per-radioRx read cost swinging 688-2238 us. The call count per frame is fixed, so the variance is in the per-call cost. (b) **Allan deviation is measurement-floor limited below tau ~ 20 s** (flat ~48 samples of apparent drift, ADEV falling as 1/tau), so the clock's true short-term stability is unmeasured -- it is better than 0.04 ppm at tau = 10 s and that is all we can say. A longer baseline or a lower-noise phase observable would resolve it; the 0.23 ppm session-scale drift dominates either way. |
| P2 | AP-55 | **The coalesced throwaway drops the sub-slot remainder of a short read, and it is latent rather than live.** `whole = got / samples_per_slot` accounts only WHOLE slots, so after a truncated read (rx_gap_break; ret=2032 against a 12288 request observed live) the stream sits `got % samples_per_slot` samples into the next slot while the slot index says its start. Safe on today's schedule and provably so: the coalesced slots are DISCARDED, and the Houdini sync path re-anchors from each read's OWN timestamp rather than an accumulated position. **NOT safe once DL data slots exist**, which is exactly what AP-45(d) says is coming: those slots would be windowed short by the remainder, and the symptom would be a mis-decoded DL slot rather than anything pointing here. Made VISIBLE 2026-09-02 `165e770` (a warning naming the remainder and saying which schedule is safe); the realignment itself is NOT written, because it cannot be validated against a DL schedule that does not exist yet. Do it with the first DL slot work, not before. Found by the code review of the AP-54 pass, DEMO_VERIFICATION 8.68. | OPEN, latent; visible, not fixed |
| P3 | AP-56 | **The tracker's env knobs have no test and no bounds check beyond finiteness.** `envDouble` now rejects NaN/inf (AP-54), but a finite nonsense value still lands: `HOUDINI_GRID_ALPHA=50` or `HOUDINI_SCATTER_TOL_US=0.001` are accepted silently and produce a UE that never locks, with the operator holding a walkthrough that says the defaults are measured values. The knobs are now documented (CSI walkthrough 7.1, 16 of them) and the geometry one of them controls is stated at bring-up (the beacon accept window, DEMO_VERIFICATION 8.65), which covers the worst case. A range check per knob, and a radio-free test that sweeps the ladder of sample rates and asserts the derived constants stay consistent, is the durable version. That test is what would have caught the kSlotCap collapse (8.65) and the kConfirmTol scaling gap (8.59) without a review. | proposed |
| P3 | AP-60 | **Role-layer gaps found by the 2026-09-02 rig campaign.** `[-> propose AG ticket]` **(1) The ship-to-rig procedure should be a skill.** Bundle over ssh, fetch, hard reset, build, then VERIFY BY RUNTIME STRING -- I ran it three times this session and it lives only as prose in lane memory (`rig-b-app-ops`). It has a fail-closed check built in (grep the binary for a string only the new code emits) which is exactly what a skill should carry, and one of my three runs picked a bad verification string and had to be re-done. **(2) Nothing in the role layer warns about the instrument-failure pattern**, which produced more defects this session than the system under test did: throttled counters read as totals, a component quoted as the whole, un-interleaved A/B on a drifting bench, a regex that could not match. `conventions/evidence.md` covers what counts as evidence but not how measurement itself fails. A short section, or a pointer to the lane memory, would have saved several hours. **(3) `tools/tracker_lint.py` is lane-local and the format it checks is a workspace convention** (`conventions/tracker.md`) -- if other lanes' trackers use the same table it belongs upstream, and if they do not, the convention doc should say the table shape is per-lane. | proposed to the coordinator |
| P2 | AP-58 | **The CNS low fraction cannot serve as a gate criterion, and the log it is read from invites a specific error.** Two forms are emitted: a periodic SUMMARY carrying `(D datagrams, L low)`, and a per-event warning THROTTLED ON A DOUBLING SCHEDULE carrying `(low occurrence K of D)`. K is only ever 1, 2, 4, 8, 16, 32, so reading it as a count produces a table whose every entry is a power of two -- which is exactly what happened across 15 runs on 2026-09-02 and was about to be filed as a regression against 8.51 (DEMO_VERIFICATION 8.85). Even read correctly the quantity varies **8x run to run on identical code** at a 1536-datagram denominator (0.20-1.76%), so it discriminates nothing at this run length. Two asks: **(a)** make the warning state that it is throttled, or carry the running total in it, so the two forms cannot be confused; **(b)** decide what run length makes the CNS fraction a usable figure, or stop quoting it as a gate criterion and quote the sync residual sd instead, which does discriminate. `gate_summary.py` now reads the summary only. | proposed |
| P2 | AP-59 | **ANSWERED 2026-09-02: THE SHORT READS ARE NOT LOSS, AND THE RATE IS NODE-DEPENDENT.** [user proposed the test: recover each sample's time and measure the gaps.] `rx_gap_census.py` computes `gap = t[k+1] - (t[k] + ret[k])` from each read's own timestamp, so zero means nothing was lost whatever the read length and nonzero is exactly the samples that never arrived. Plain continuous RX, no TDD armed, 3000 reads per node: **.21 short 12.7 % (min 1072), .22 short 57.1 % (min 1648), and ZERO nonzero gaps in 5998 consecutive pairs across both.** `rx_gap_break` changes nothing: 58.2 % short set against 58.0 % clear, zero gaps either way, so these are not gap-break truncations at a seam. **They are EARLY RETURNS with nothing missing, and a count of short reads is not a loss measurement** -- which also retires the worry in the original row. What REMAINS unexplained is the **4.5x difference between two nodes running identical code on the same host**; the only asymmetry visible from this lane is the data interface (192.168.5.9 vs 192.168.6.8). Handed to the software lane as a characterised behaviour rather than a suspicion. Also note the stored correlation ran the WRONG way for loss -- runs with many short reads had MORE detections/s (93.9 vs 61.5) and LESS jitter (7.5 vs 11.2 samples) -- which is why this was measured rather than assumed. `[-> propose SH ticket for the node asymmetry]`  **COALESCING IS RULED OUT 2026-09-02: both .64 ports carry IDENTICAL settings** (adaptive RX on, rx-usecs 8, rx-frames 128, GRO on, LRO off, MTU 9000), checked by the software lane after I proposed closing this row on exactly that hypothesis. I had offered to close on a difference I never verified existed; they looked and it does not. Remaining candidates, none tested: interrupt PLACEMENT (which CPU serves each port's queues relative to the reader), and the node's own frame cadence. Their SH-350 carries the driver half. ****ANSWERED AND CLOSED 2026-09-02 by exactly that sweep.** Two sweeps an hour apart, 8 request sizes from 2048 to 49152, both nodes, with the gap column the first sweep was missing. **ZERO gaps in all 9592 consecutive read pairs at every size on both nodes**, so a short read is an early return with nothing missing and a short-read count is not a loss measurement -- at any request size, not just the 12288 the original claim rested on. The rate RISES with request size on both nodes and the two agree at six of eight sizes. **And the 4.5x node asymmetry is HOST LOAD**: the same node at 32768 read 96.8 % in one sweep and 35.0 % an hour later, and at 24576 74.6 % against 22.6 %. The curve's shape reproduces, its magnitude does not. That accounts for the stored 12.7 % vs 57.1 % and for its inversion without any node-level mechanism, and retires the interrupt-placement and frame-cadence hypotheses as unnecessary rather than untested. Nothing is left for the software lane to explain, so the handoff is withdrawn rather than delivered. DEMO_VERIFICATION 8.132/8.133. | ANSWERED (not loss); asymmetry OPEN, coalescing ruled out |
| P2 | AP-57 | **The BS-side pilot arrival is a HEALTH CHECK today and AP-51 needs it as a MEASUREMENT.** `pilot_grid_off` is printed under `HOUDINI_BS_RX_DEBUG` and read by eye. Two things landed to make it usable as the second observable in the two-way transfer (`c2d073e`, `e0bddc2`): `HOUDINI_BS_RX_EVERY` sets the throttle, previously hardcoded at 20 and far too sparse for a slope fit, and the line now carries `stamp_ticks` because the SLOPE is what AP-51 needs and the frame counter cannot give it (it counts PROCESSED frames). Deliberately a separate knob from `HOUDINI_BS_RX_DEBUG`, which has meant on/off in the runbook, the walkthrough, `ap15_correlate.py` and `run_pad_campaign.sh` since it was added. **What is still missing is the promotion itself:** the value is parsed out of a log by `two_way_transfer.py` rather than carried as telemetry, so it cannot feed a loop. Wire it into the SYN1 path (or its own record) once leg 8 shows the de-embed works on real data -- not before, because a wired bench cannot show Doppler separating and a measurement nobody has validated should not gain a consumer. -> AP-51, AP-41. | proposed, blocked on leg 8 |
| P2 | AP-62 | **A GATE CAN BE INVALIDATED WITHOUT FAILING, AND THE RIG HAS NO BOOKING PROTOCOL.** `[-> propose AG ticket]` On 2026-09-02 the software lane rolled a device+host build onto .21/.22/.64 between runs 2 and 3 of my interleaved PRE/POST gate; all five runs were green and nothing in the output said the baseline had moved. Both lanes had been told the rig was theirs. Their HIL legs were also refused twice by an RX stream I held. Two separable needs. (1) **Detection, done in this lane**: `gate_summary.py` parses the node-stack line from every run and exits 3 when runs disagree on fpga_commit / device_build / host_build / beacon type; their HIL wrapper already does the equivalent with `HOUDINI_EXPECT_DEVICE_BUILD`, so the pattern is proven on both sides and belongs in the conventions rather than in two tools. (2) **Prevention, needs the coordinator**: there is no shared way to claim the bench. A file-based lock on .64 naming the holder and the window, checked by every device-touching entry point, would have turned a silent baseline change into a refusal. Cheap, and the cost of not having it is a campaign silently measured against two different stacks | UNDELIVERED handoff |
| P3 | AP-63 | **'Radios Not Found' ROOT-CAUSED 2026-09-02: TWO causes, distinguishable in the log, neither of them device nondeterminism.** (1) **A LEFTOVER RX STREAM.** `csi_server.py --launch` runs the sounder as a grandchild under a bash retry loop; killing the launcher orphans the loop, it restarts a sounder, that sounder holds both boards, and every later run is refused at `setSampleRate(RX): an RX stream is open` while writing an EMPTY log -- which reads as 'nothing happened', not as an error. Six consecutive gate runs and three probe legs died that way before the empty logs were recognised as failures. The launcher's four retries are why it looked like '1 in 4'. **A bare sounder releases both boards on SIGINT AND SIGKILL**, so it is never a server-side leak and no `systemctl restart` is needed. (2) **An RPC TIMEOUT on a cold make** (`SoapyRPCUnpacker::recv() TIMEOUT`) -- a cold make takes 3.34 s against a 1 s default, the class already recorded in the probes. **Both print the same 'Radios Not Found. Will attempt a retry' banner, so the banner cannot attribute a cause; the line above it can.** Straight application of the stored-label rule. FIX SHIPPED for (1): `tools/rig_release_holders.py` clears it without sudo and CSI_DEMO_WALKTHROUGH 8.0 documents the symptom and the commands. Automatic recovery is AP-64. DEMO_VERIFICATION 8.127, 8.131. -- CLOSED -- cause known, operator fix shipped | OPEN |
| P3 | AP-65 | **Role-layer proposals from the 2026-09-02 campaign.** `[-> propose AG ticket]` (1) **`pkill -f` / `pgrep -f` must not be used from a session's own shell**: the pattern appears in the invoking command line, so it matches and kills the caller. It killed an ssh session mid-command and a running gate script on the same afternoon. `tools/rig_release_holders.py` scans `/proc` and excludes its own ancestry instead; the RULE belongs in the conventions, not only in one tool. (2) **Ad-hoc output extraction by row position is a measurement bug waiting to happen** -- a `sed -n "6p"` on a tool's table reported 0 accepts for a healthy 19-accept run after that tool gained three header lines the same day. Parse by a stable key, or have the tool emit json. (3) The ship-to-rig bundle procedure is still retyped every session (see AP-60). (4) **A prediction that misses means checking the implementation first** is now binding user feedback and belongs beside the measurement-discipline convention. (5) [2026-09-03] **Pre-register, then AMEND before re-running, never after**: the 8z block worked twice in one campaign -- the criteria were written before the runs, a confound found in the first two runs (the SNR floor ranking shapes by PAPR, 8.157) was recorded as an amendment BEFORE the amended runs, and a saturated result was extended the same way; each amendment says what the previous runs could and could not show. One paragraph for `conventions/evidence.md`: a criterion changed after its runs is a result, not a criterion. (6) [2026-09-03 pm] **A bench probe must open the radio exactly as the sounder does, stream arguments included** -- the phase probes omitted `mts=true` and measured a per-arm converter draw as if it were physics for most of a day (8.171); the fix is one line per probe, the convention is "mirror `Radio.cc`, do not re-derive". (7) `run_shape_campaign.sh` + `shape_campaign_summary.py` are the lane's interleaved-campaign driver now (Latin square, wall-clock runs, teardown per run, gate_summary at the end); the ship-to-rig bundle step in (3) is still retyped | UNDELIVERED handoff |
| P2 | AP-67 | **Phase across the frame: the beacon alone carries it to roughly 5 degrees, and that is the number that decides whether downlink pilots are needed.** Literature review 2026-09-03 (`docs/UE_TIME_FREQ_SYNC.md` section 11): every system that runs independent oscillators from an over-the-air reference (AirSync, MegaMIMO, BeamSync, the two-way-time-transfer work, the TDD phase-calibration work) uses a periodic reference, a two-state phase/frequency estimator with a random-walk process model, and PREDICTION between references, with the reference interval set by the phase-noise spectrum; NR and 802.11 additionally re-measure phase INSIDE every data slot (DMRS/PTRS, four pilot tones) and never carry it from the sync block. Our own numbers: the frame-to-frame beacon phase is coherent (AP-34(b): R 0.99, circular sd 0.10-0.13 rad at 1 ms), and the tracked clock is good to 0.02 ppm, so a beacon-only phase tracker predicts the carrier to about 7 degrees per ms worst case and 4-5 degrees rms mid-frame. That supports QPSK/16-QAM downlink demodulation with no in-slot pilot and the per-frame phase anchor AP-42 needs; it does NOT support 64-QAM (about 2 degrees needed) or anything coherent across nodes. So: (a) build the two-state phase/frequency tracker on the beacon when downlink data symbols exist (AP-45), the phase twin of the AP-31/41 timing tracker; (b) plan a few pilot tones per downlink data symbol for the common-phase-error term, as the MATLAB reference already does; (c) the cheap measurement that sizes (b) BEFORE (a) is the beacon-phase innovation against elapsed frames k = 1..8, which `beacon_phase_coherence.py` measures at k = 1 today and needs a `--lag-frames` axis for. -> AP-42, AP-45, AP-34(b) **MEASURED 2026-09-03 (8.168): innovation 0.021-0.026 rad flat to lag 4 frames, so the beacon carries phase to ~1.5 degrees over 4 ms on this bench -- 64-QAM-grade without in-slot pilots here; pilots remain the answer for channel change and other hardware.** Design rule from the root cause of the first, ambiguous run (8.166-8.167): the PhaseTracker reads phase at the FRACTIONAL peak or at the tracked grid's fractional position, never at an integer argmax, because the received beacon's correlation lobe carries a 0.06-0.30 rad phase step between adjacent samples (dispersion, both capture paths; 8.167) and one run showed 2.4 rad (AP-70). (c) is DONE at k = 1..4; k up to 8 and over the air remain. | MEASURED; tracker build pending DL data (AP-45) |
| P3 | AP-68 | **SSB-lite: the part of NR acquisition that `nr_pss` still leaves out, costed.** After AP-66 the remaining differences from NR's cell search are, in the order they would matter over the air: no SSS (our false-lock guard stays the in-window SNR floor plus the grid residual; NR's is a decoded second sequence), no cyclic prefix on the PSS (an echo contaminates the symbol instead of rotating it), the PSS filling the whole band rather than 127 of 240 subcarriers, and frequency from the TRS repetition rather than the PSS-to-SSS channel-estimate phase. All four close with a PSS + SSS pair at the 240 kHz numerology with NR prefix lengths (512-point symbols, 36-sample prefix, 4 x 548 = 2192 samples, inside the 4096-sample replay RAM): SSS generation is thirty lines on the 38.211 recurrences `beacon_shapes.h` already has, the frequency-domain SSS confirm and the PSS-to-SSS CFO estimator (srsRAN's `cfo = arg(corr_sss conj(corr_pss)) / 2 pi dt`) are one FFT each, and nothing in the driver or gateware moves. About a day. Why not link srsRAN/OAI instead: both assume the 240-subcarrier block with PBCH, whose SSB index and MIB exist to tell a UE where in the frame it is, which our fixed beacon position settles; at 30 kHz one block is 17536 samples, over four times the replay RAM. Take up only if the OTA target wants NR-shaped acquisition; AP-66's answer does not need it. -> AP-66 | OPEN |
| P2 | AP-69 | **The beacon CFO estimator reads the burst's edge under fractional timing; give it a margin now and the waveform a cyclic postfix in the library.** DEMO_VERIFICATION 8.164: `Receiver::estimateCFO` places its two windows from the detector's integer index and the shipped +8 slide (AP-39) was derived on an integer-delay model where the samples after the beacon are zero. On a real link they are the interpolation tail of the edge, and the estimator multiplies real samples by it: up to +4.9 kHz of bias for the 64-sample TRS pair, +0.3 kHz for the 128-sample gold field, swinging with the fractional delay the free-running clock walks through every frame. That is nr's 3220 Hz scatter (8.114) explained; legacy's 996 is not. (a) Immediate: replace the +8 slide with a symmetric margin (12 samples measured adequate for L 64, within 1.3 thermal sd), mirrored in `beacon_shape_compare.py`'s `cfo_from_fine`; the estimator is telemetry today (the tracked clock is the CFO source, AP-31d), so the change is low risk and gates on the offline sweep plus one interleaved run's `bcfo sd`. (b) Durable: a `postfix_len` in `beacon_shapes::Desc` (the first g samples of the fine symbol appended after the last repetition) so the window can slide +-g and see only cyclic content, as every symbol in an 802.11 or NR burst is flanked by cyclic content; `replica_tail()` already carries the index convention for it. Ships as a variant (`legacy_post`) and is measured interleaved before any default moves. Both belong in the sync library (AP-70) rather than as more env knobs | OPEN |

| P2 | AP-72 | **MEASURED AND FIXED 2026-09-03 (DEMO_VERIFICATION 8aj, 8al; gate 8ak pre-registered, NOT YET RUN): the share is 23 % to 36 % of arrival phases by shape, and the fix is `sync.detector.first_path_guard` = 1, which skips the peak's split partner and returns four of the five shapes to the ideal rounder (0.289 samples RMS, from 0.372 to 0.458). Default stays 0 until the silicon gate. The OTHER named fix, a sub-sample peak fit, was built twice and withdrawn twice and is now AP-75.** ORIGINAL: the first-path pick toggles by one sample under fractional timing: the split matched-filter peak's earlier neighbour clears the -9 dB floor on 7 of 24 golden windows (statistic 4-15 dB below the peak), and the fraction drifts with the clock, so the pick flips as it crosses the floor (DEMO_VERIFICATION 8.177).** Candidate source of the ~1-sample adjacent-difference jitter every campaign reports. Measure its share first (offline: the 24 windows and 8.164's fractional-delay sweep with the statistic; on silicon: jitter with `sync.detector.first_path_floor_db` at -3 against -9, interleaved), then fix with a sub-sample peak fit or a floor judged against the two-sample split energy, gated like AP-34 (index convention must not move). The fixtures' `statistic` field is the instrument | FIX LANDED, silicon gate 8ak PENDING |
| P2 | AP-74 | **P5 of the sync library: the phase tracker (`docs/SYNC_LIBRARY_ARCHITECTURE.md`), the piece AP-71 left unbuilt.** Track the beacon's carrier phase across the frame so the demodulator can decide when downlink pilots are needed (AP-67 measured the beacon alone carries phase to about 5 degrees mid-frame, to lag 4; k up to 8 and over the air remain). Build it in the library with a radio-free test against `sim::Channel`, then measure on silicon with the AP-67 instrument. Depends on the two-way measurement (AP-45/51) for the OTA claim, not for the library piece | OPEN 2026-09-03 |
| P2 | AP-75 | **A sub-sample position for the detected path, which two three-point estimators failed to deliver and which needs the replica's own autocorrelation.** Built and WITHDRAWN on `fix/first-path-split-peak` (DEMO_VERIFICATION 8ah, 8ai, 8aj, 8al): a parabola on the correlator amplitude carries an S-curve bias to 0.234 samples because the lobe is nearly a delta, and the ratio of the bracketing pair, exact for an ideal sinc and a triangle, is defeated by the replicas' own ZERO-DELAY ASYMMETRY (legacy reads 0.0079 one side of the detected index and 0.0024 the other; nr 0.0157 and 0.0305). Near a whole-sample arrival that fixed asymmetry outweighs the delay's, the side selection flips, and the refined position is WORSE than the raw integer on 12 to 26 per cent of timing points, with the wrong sign on up to 23 per cent -- the failure lands exactly at lock, where a tracker would use it. What a replacement needs: the expected correlation triple as a function of sub-sample offset, computed once from the replica at Detector construction, and the offset estimated by matching the measured triple against it. Gate: better than the argmax's own quantisation (RMS below 1/sqrt(12) = 0.289 samples) AND no worse than the raw integer at ANY timing point, both stated before the runs; the geometry test's lobe table is the specification and its channel simulator the instrument. Consumers waiting on it: AP-67 (phase read at the fractional peak) and AP-74 (the phase tracker) | OPEN 2026-09-03 |
| P2 | AP-76 | **Our demo start reprograms the TX rate immediately before opening a live TX stream, which the software lane has now shown DETERMINISTICALLY dirties the device (SH-335).** Their evidence: a soak dirtied in 17 of 17 hammer sessions that did a real TX rate reprogram and 0 otherwise, on the current stack, so the standing workaround (do not change the TX rate right before opening a live TX stream) is firm rather than precautionary. `RadioHoudini` sets pre-stream rates and the NCO and then sets up the TX stream, so the sounder hits the pattern on every run and has been protected only by the teardown ladder between runs. Fix on OUR side rather than relying on the workaround: establish the rate before any stream object exists, or open the stream first and leave the rate alone, whichever the driver contract allows. Gate: the ordering change is invisible at the shipped defaults (accepts, escalations, residual, startup record unchanged) over interleaved legs, and a hammer sequence that dirtied before does not after. Cross-ref SH-335, SH-348 item 3 | OPEN 2026-09-03 |

## Closed

- **AP-24** - Docs refresh - DONE: CSI_DEMO_WALKTHROUGH rewritten against DEMO_VERIFICATION.md rows 4.12-4.49 (model, expected logs, tuning knobs, troubleshooting causes). Decision recorded: the re-sync detection log line STAYS (INFO, fires only on targeted attempts, proved diagnostic in the 4.42 investigation). Triage [user 2026-08-30]: AP-20 is a real need (killed 2 of 5 campaign runs; raised to P1); AP-21/22 are nice-to-haves.

- **AP-27** - UE TX landing quantization - CLOSED via the stronger fix: one zero-padded burst per frame (the burst interior escapes the 3125 ns anchor grid entirely), not the 3-slot spacing move. Both snap draws verified dead across 4 restarts; tx_advance recalibrated to 247; seat now nominal +128 exactly (DEMO_VERIFICATION.md 4.44/4.48 on feat/csi-gui-tabler). Design fact kept: the 384-tick grid is the ns-API seam, hardware TX granularity is 8 ticks -- a tick-native protocol revision remains a possible future SH ask.

- **AP-66** - The NR acquisition architecture on the `nr` burst (`nr_pss`: PSS as the replica, plain matched filter): the 5 dB NR deficit was the detector it was run through, not the waveform - CLOSED 2026-09-03 `1c3e434` (DEMO_VERIFICATION 8.154-8.162; 42 silicon runs, acquires/tracks like legacy at every reachable level; P6 margin UNDECIDED on this cable, offline proc gain 37.8 = legacy 37.7 > nr 31.8; one rejected noise-window crossing per hunt is its cost -> AP-68; phase -> AP-67). `legacy` stays default.
- **AP-15** - Constellation good/ring per restart - CLOSED: 10/10 restarts at 0.989-0.990 after the full mechanism stack was found, fixed, and verified (anchor lottery -> confirmed acquisition + SNR floor; per-run timing offset -> two-stage recovery; pilot-slot H ISI + per-boot converter draw -> MTS adoption [user]). Evidence: DEMO_VERIFICATION.md 4.31-4.41 on feat/csi-gui-tabler; final campaign ap15_mts. Nine falsified hypotheses along the way are recorded there and in the arc memory.

- **AP-23** - MTS adoption (both nodes) - DONE `072d7d6`+`d6429d1` (mts=true all streams, TX-first order, ch0 membership stream for the DAC tile-0 RefTile contract; TARGET_LATENCY verified engaged live BS 320 / UE 280). Follow-on resync redesign (liveness model + grid-targeted search) in `cc526a5`+`d87ad83`; steady state 12/12 alive at 31.8 dB, 0 escalations (ledger 4.40).

- **AP-18** - Resync escalation to full re-acquisition - DONE `435659b`+`0944f6c` (2 exhausted episodes or >=4 unmatched holds -> houdiniAcquireAnchor; BS reports UE PILOT LOST/RETURNED). Verified end-to-end on a live 15 s beacon outage: escalate -> hunt -> re-anchor with IDENTICAL grid alignment before/after (ledger 4.34).

- **AP-17** - TDD framer TX gate 15x coarser than a sounder slot (beacon covered slots 0..14) - DONE `c64a557` (slot-granular ring from the config: one 4096-tick symbol per slot, single-copy beacon in the B slot; verified on silicon, DEMO_VERIFICATION.md 4.12/4.16/4.21).

Landed rows collapse here (one line; the verbose body stays in git):

- **AP-14** - CSI live dashboard: Tabler restyle + per-subcarrier repeat quality + raw-ADC tab - DONE PR #2 `4366f6b` (became the whole live-demo dashboard: phase stack, ADC envelope in dBFS, H-stability, CNS score counter, sync/CFO panel).
- **AP-19** - Re-derive tx_advance from the measured pilot arrival - DONE PR #2 `4366f6b` (tx_advance 205 -> 247 once the zero-padded burst made placement sample-exact; pilot_grid_off -1, pu_spacing_err 0 across 125 frames. The row's +-120 band was the pre-burst snap draw, DEMO_VERIFICATION.md 4.48).
- **AP-30** - Beacon CFO estimator rewritten and validated - DONE PR #2 `4366f6b` (two-stage Schmidl-Cox, coarse STS lag 16 resolving the fine gold lag-128 ambiguity; sign and scale validated on silicon by deliberate injection. The apparent bias was root-caused as a fixed phase error divided by a short lag, NOT a frequency, so it must not be calibrated out; the fix is lag. -> AP-31).
- **AP-32** - Sync/CFO live panel - DONE PR #2 `4366f6b` (SYN1 wire kind from the UE sync thread, one datagram per resync detection, six badge states, CFO beside the residual; 44-byte wire covered by tests/demo-verify/syn1_wire_test.py).

- **AP-9** - Frequency-lock the two RFSoC4x2 boards (external `CLK IN` mux select) - RESOLVED: firmware `CLK_SEL0/1` external-mux path landed via the software/os lanes; verified app-side by the pilot no longer walking the BS frame and the two-tone scope beat stopping. Evidence + options ruled in/out: `docs/TWO_BOARD_CLOCK_LOCK.md`. The absolute CFO figures this row used to quote were struck 2026-09-01 as unmeasured rather than merely old (unresolved lag-128 estimate, no coarse stage, at 93% of that lag's fold limit); see that doc's RESOLUTION note. AP-33 measures it properly.
- **AP-7** - xsk ingest refinements from the V4 evidence (batch acquire/release, starvation-exit wake, optional busy-poll worker mode) - DELIVERED -> **SH-265** (SoapyHoudiniSDR; all three asks bridged verbatim, cross-linked to AP-7 + SH-258 + SH-254). Pure handoff row, so it closes on DELIVERY, not on the receiver finishing (`conventions/handoffs.md` step 2); the driver work is reduced-priority there because the app already reaches 99.998 % via readStream. Coordinator-closed.

- **AP-10** - VALIDATED ON SILICON 2026-09-02, and I nearly missed it. The fix fired in all 21 sounder runs of the rig sessi - DONE PR #3 `bfa0bb9` (2026-09-03) (CSI view drop marking validated on silicon, 21 runs)

- **AP-11** - Sounder bring-up diagnosability + retry robustness. Bench session 2026-08-28 lost hours to failures that repor - DONE PR #3 `bfa0bb9` (2026-09-03) (bring-up diagnosability + retry robustness, from `feat/correlator-testing`)

- **AP-12** - CSI dashboard: stale marking and fixed axes. [user] The live view held its last good estimate with no indicati - DONE PR #3 `bfa0bb9` (2026-09-03) (dashboard stale marking + fixed axes)

- **AP-31** - CLOSED ON SILICON 2026-09-01 on `arc/free-run-sync` (`feat/internal-clock-cfo`) -- retire at merge. Both nodes - DONE PR #3 `bfa0bb9` (2026-09-03) (UE keeps time sync on free-running clocks, closed on silicon 2026-09-01)

- **AP-33** - MEASURED 2026-09-01 on `arc/free-run-sync` -- retire at merge. The onboard references are `.22` +7.20 ppm and - DONE PR #3 `bfa0bb9` (2026-09-03) (onboard references measured: `.22` +7.20 ppm, `.21` -1.33 ppm)

- **AP-34** - (b) FIXED AND VALIDATED 2026-09-02. The beacon IS coherent and the bug was mine. I had inferred the replay's c - DONE PR #3 `bfa0bb9` (2026-09-03) ((a) and (b) fixed, VERIFIED-HW)

- **AP-35** - SUPERSEDED 2026-09-01 by the software lane's SH-341: both nodes are now `clock_ref = calibrated` (`.21` cal_da - SUPERSEDED -> SH-341 (both nodes `clock_ref = calibrated`)

- **AP-36** - SWEEP FINISHED 2026-09-02 `165e770`. Demo-verify instruments opened devices with `timeout="1000000"`, which is - DONE PR #3 `bfa0bb9` (2026-09-03) (the 1 s make() deadline sweep; a recv TIMEOUT on make is this, not AP-20)

- **AP-37** - ANSWERED 2026-09-01, and this row was left stale until 2026-09-02. DEMOTED P0 -> P3, kept only for the trap it - ANSWERED, DEMOTED, retired PR #3 `bfa0bb9` (2026-09-03) (the rotation is real; the measurement trap is in the row's history)

- **AP-38** - DONE + EVALUATED, and the honest read is 'keep, but modest': 13 vs 15 low frames of 2048 at zero offset, 31 vs - DONE PR #3 `bfa0bb9` (2026-09-03) (evaluated: keep, modest)

- **AP-39** - estimateCFO index guard: +8 samples, one constant, 47x less bias. Root-caused offline (no hardware): the bias - DONE PR #3 `bfa0bb9` (2026-09-03) (estimateCFO index guard +8, the `CFO guard 8` on every startup line)

- **AP-40** - DONE: tolerance now expressed in TIME (default 8.3333 us = the 1024 samples it meant at the tuned rate, so beh - DONE PR #3 `bfa0bb9` (2026-09-03) (tolerance expressed in time)

- **AP-41** - MEASURED ON SILICON 2026-09-02: the kalman is ~1.7x better, and its innovation gate is UNPROVEN in the field - DECIDED, retired PR #3 `bfa0bb9` (2026-09-03) (alpha-beta stays the default; the Kalman is kept, its innovation gate unproven in the field)

- **AP-42** - LARGELY SUPERSEDED by AP-47: steering the UE's own reference nulls its transmit frequency error at the source - SUPERSEDED -> AP-47

- **AP-47** - UNITS, STATED EXPLICITLY BECAUSE THEY CAUSED A CROSS-LANE ERROR 2026-09-02: -0.1251 ppm/count is in EPS UNITS - DONE PR #3 `bfa0bb9` (2026-09-03) (VERIFIED, units stated)

- **AP-48** - DONE + VERIFIED: -0.1251 ppm/count measured with the beacon arrival ramp over a 6-point sweep, confirming the - DONE PR #3 `bfa0bb9` (2026-09-03) (-0.1251 ppm/count measured over a 6-point sweep)

- **AP-49** - DEMOTED AND LARGELY WITHDRAWN 2026-09-02 [user pushback, then traced in source]. The TX schedule has NO ns ari - DROPPED -> the TX-schedule accumulation claim withdrawn (no ns arithmetic in the schedule); the ensemble-boundary caveat stays in DEMO_VERIFICATION

- **AP-54** - ALL NINE ITEMS FIXED IN CODE 2026-09-02 on `feat/internal-clock-cfo` `165e770`; VERIFIED-CODE, awaiting the ga - DONE PR #3 `bfa0bb9` (2026-09-03) (nine items fixed, gate run passed 2026-09-02)

- **AP-61** - `tools/tracker_lint.py` added, and the reason is the point. The BACKLOG table check was retyped inline SIX tim - DONE PR #3 `bfa0bb9` (2026-09-03) (`tools/tracker_lint.py`)

- **AP-64** - FIXED AND VERIFIED 2026-09-03 (DEMO_VERIFICATION 8.195, 4 of 4 on silicon): `csi_server.py --launch` supervise - DONE PR #3 `bfa0bb9` (2026-09-03) (launcher supervises the sounder with PR_SET_PDEATHSIG; 4 of 4 on silicon, DEMO_VERIFICATION 8.195)

- **AP-70** - ROOT-CAUSED AND CLOSED 2026-09-03 (8.172), six against six: the bench probes opened the UE's receive stream ou - DONE PR #3 `bfa0bb9` (2026-09-03) (root-caused: the probes' UE receive stream outside the MTS group, 8.172)

- **AP-71** - The sync library (`houdini_sync`): roll the beacon, detector, confirm, carrier and tracking code and every mea - DONE PR #3 `bfa0bb9` (2026-09-03) (the sync library `houdini_sync` landed with the beacon shapes, the detector evidence, the geometry, the resync policy, the pfa bar and the radio platform seam; P5 phase tracker -> AP-74)

- **AP-73** - MEASURED AND CLOSED 2026-09-03 (DEMO_VERIFICATION 8.194): the reload lands without TX_CLEAR on the current sta - DONE PR #3 `bfa0bb9` (2026-09-03) (measured: the reload lands without TX_CLEAR, 4.24 did not reproduce, 8.194)

- **AP-16** - RX is ~30 dB under-driven and no gain control is exposed to fix it. `[→ propose SH ticket]` Measured on the li - handed off -> SH-345 (RX under-driven, no gain control exposed)

- **AP-20** - SoapySDRServer session wedge. `[-> propose SH ticket]` 4x on 2026-08-30 (.21 x2, .22 x2, ~1 in 8 client sessio - handed off -> SH-346 (SoapySDRServer session wedge)

- **AP-21** - Hardware-chained windowed RX (gated slots on a continuous stream). `[-> propose SH ticket]` The target BS sche - handed off -> SH-347

- **AP-22** - Driver-anomaly evidence pack from the verification campaign. `[-> propose SH ticket]` (1) Abort-only pre-arm c - handed off -> SH-349 (driver-anomaly evidence pack)
